[phases.setup]
nixPkgs = ["python311", "chromium", "chromium-unwrapped"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pip install playwright --break-system-packages || true"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
